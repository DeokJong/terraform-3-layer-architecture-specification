[project]
name = "terraform-3-layer-architecture-specification"
version = "0.1.0"
description = "Documentation maintenance utilities for the Terraform 3-layer architecture specification"
requires-python = ">=3.12"
dependencies = [
  "pyyaml>=6.0.2",
]

[tool.uv]
package = false
